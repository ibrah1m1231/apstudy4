{
  // Only needed if Cloudflare insists on a deploy command (the Workers path).
  // If you use Cloudflare Pages instead, you do not need this file at all.
  "name": "ap-study-hub",
  "compatibility_date": "2026-09-11",
  "assets": {
    // index.html sits at the root of the repo
    "directory": "./"
  }
}
